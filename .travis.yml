language: c

compiler: gcc

script:
    - make env_prepare
    - sudo ln -sf toolchain/bin/* /usr/bin/
    - echo `pwd
    - env
    - . ./env.sh;
    - env
    - loacte arm-none-eabi-gcc
    - cd src; scons

before_install:
    test -n $CC && unset CC
