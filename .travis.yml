language: c

before_install:
    test -n $CC && unset CC

before_script:
    - make env_prepare
    - sudo ln -sf toolchain/bin/* /usr/bin/
    - . ./env.sh;
    - env
    - which arm-none-eabi-gcc

script:
    - scons -C src

